<image wx:if="{{ src }}" class="icon" src="{{ src }}" mode="aspectFit"
        style="width:{{size}}rpx;height:{{size}}rpx"></image>
<view wx:else class="icon icon-ph" style="width:{{size}}rpx;height:{{size}}rpx"></view>
